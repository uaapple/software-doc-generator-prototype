# 第一阶段方案：基于系统需求与模型产物自动生成软件开发需求原型

## Summary
目标是先做一个内网 Web 原型，打通“客户系统需求 PDF + 模型侧产物”到“可审核的软件开发需求条目草稿”的最小闭环，用于节省补写软件开发需求的人力。
第一阶段以正确性和可追溯性为核心，不追求一次性全自动定稿；系统先自动生成完整草稿，再由工程师统一审核。

## Key Changes
### 1. 输入与处理范围
- 输入源固定为 3 类：客户系统需求 PDF、模型导出的 PDF、由 Simulink 生成的 C 文件。
- `slx` 文件第一阶段不直接解析，但在架构上预留模型解析模块接口，后续可接入。
- 任务模式按“单项目单次生成”设计：一次上传一套输入物，生成一份软件开发需求草稿。

### 2. 核心流程设计
- 建立文件接收与项目任务容器：上传、分类、版本标记、任务状态跟踪。
- 对系统需求 PDF 做文本抽取，默认电子版文本抽取为主，OCR 仅作兜底。
- 对模型 PDF 和 C 文件做结构化信息抽取，重点抽取功能描述、接口/信号、状态/模式、阈值/标定、异常/保护、时序/周期等候选事实。
- 增加“证据归并与冲突检测”层：把系统需求作为最高优先级事实源，模型 PDF 与 C 文件作为实现细化和佐证来源；冲突项不自动裁决，标记给人工审核。
- 建立“需求生成”层：基于你们现有软件开发需求模板，把抽取结果映射成带编号的软件需求条目，而不是只生成自由文本。
- 建立“追溯层”：每条软件需求必须绑定来源文件、页码/章节、证据片段和来源类型。
- 建立“审核层”：支持人工查看、修改、接受/驳回需求条目，并保留原始证据引用。

### 3. 平台形态
- 采用本地/内网 Web 架构，优先支持上传文件、发起生成、查看结果、按条审核。
- 首版页面最少包含：项目创建页、文件上传页、任务处理页、需求结果页。
- 输出以结构化结果为主，前端展示可审核需求清单；导出 Word/PDF 可放在下一步，但数据模型要预留导出字段。

### 4. 关键接口与数据结构
- 项目实体：项目基础信息、输入文件清单、处理版本、任务状态。
- 抽取结果实体：来源文件、位置、原文片段、结构化标签、置信度。
- 软件需求实体：需求编号、标题/描述、约束条件、来源依据、冲突标记、审核状态。
- 追溯实体：需求 ID 到证据 ID 的映射，支持一对多关联。
- `slx` 预留接口：定义“模型抽取结果”统一中间格式，后续无论来自 PDF、C 还是 `slx`，都归一到同一数据结构。

## Test Plan
- 上传一套完整输入物后，系统能生成一份结构化软件开发需求草稿。
- 每条生成的需求都能回溯到至少一个来源证据，且能显示文件名与页码/章节。
- 当系统需求与模型侧信息不一致时，结果中能显式标记冲突或低置信度项。
- 当模型侧只有 PDF + C、没有 `slx` 时，流程仍能完整跑通。
- 对电子版可选中文本 PDF，抽取成功率满足原型验证需要；抽取失败时可回退到 OCR。
- 人工审核后，需求条目状态能被保存，并保留修改前后的证据关联。
- 使用 1-3 份你们现有模板/历史样例做对齐验证，确认章节结构和条目粒度符合内部习惯。

## Assumptions
- 第一阶段默认输出中文软件开发需求。
- 第一阶段以“可审核需求条目”为主，不要求一步生成最终版正式文档排版。
- 系统需求是最高优先级事实源，模型 PDF 与 C 文件主要用于补充和验证。
- 你们可以提供至少 1 份现有软件开发需求模板或历史样例，作为输出结构对齐依据。
- `slx` 解析被视为第二阶段能力，第一阶段只做接口预留，不纳入必须交付项。
