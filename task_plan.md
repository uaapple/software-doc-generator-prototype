# 任务计划：软件需求生成平台分层导航与模块化工作台

## 目标
在保留现有软件需求/详细设计生成能力可用的前提下，把系统改造成清晰的“工程 -> 功能模块 -> 任务”分层导航结构，并让功能模块页成为主浏览入口、旧生成页回归为纯生成工具页、单次任务详情页承担审阅与采纳动作。

## 当前阶段
阶段 6

## 各阶段

### 阶段 1：需求与发现
- [x] 理解用户对内部工具工作台的定位
- [x] 明确首页需要保留品牌识别，但避免宣传页风格
- [x] 明确 LLM 接入需要支持可切换、可新增、可扩展
- [x] 将关键发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：页面结构与品牌收敛
- [x] 将首页从展示型 hero 调整为内部工作台布局
- [x] 接入 ITK logo，并按品牌蓝灰色系统一视觉
- [x] 修复中文乱码与基础信息层级
- [x] 调整上传控件风格，统一按钮与表单视觉语言
- [x] 修复卡片宽度不一致问题，统一为两列等宽栅格
- [x] 将 Skill Refinement 工作台重做为与首页一致的内部系统风格
- **状态：** complete

### 阶段 3：LLM 配置与后端接入
- [x] 新增 LLM 配置持久化存储
- [x] 增加模型配置查询、新增、默认切换接口
- [x] 首批支持 OpenAI 与豆包两类 provider
- [x] 将生成链路改为按所选模型配置调用
- [x] 记录最近一次生成所使用的模型信息
- [x] 保留并自动补回默认豆包 seed 配置
- **状态：** complete

### 阶段 4：驳回反馈闭环与 ReplayTask
- [x] 将 requirement 驳回扩展为结构化反馈采集
- [x] 新增 `RejectionRecord` 持久化与列表/详情/分组接口
- [x] 新增 `ReplayTask`，支持从驳回记录或分组发起回投任务
- [x] 新增 `SkillRule` / `SkillRuleChangeLog` / `SkillBundleRuleIndex` 基础能力
- [x] 新增反馈池页面，支持查看驳回记录、发起 replay task、审核 proposal item 并应用为 candidate bundle
- [x] ReplayTask 已接入现有 LLM Profile，创建任务时可选择已配置模型服务
- [x] 未选择模型时保留本地 fallback proposal 生成器
- [x] 自动化验证通过：`node tests/run-tests.js`
- **状态：** complete

### 阶段 5：模块化工作台基础重构
- [x] 将数据模型从项目级扩展为“工程 / 模块 / 文档空间 / 任务 / 已采纳结果”
- [x] 新增模块资产、模块任务、已采纳结果相关接口
- [x] 实现迁移演示工程脚本，将旧项目与 skill refinement 记录导入新模型
- [x] 自动化验证通过：模块任务、接受快照、当前版本编辑等场景已覆盖
- **状态：** complete

### 阶段 6：分层导航与页面职责重划
- [x] 将首页改为纯工程列表页，只保留“进入工程 / 创建工程”
- [x] 新增独立创建工程页
- [x] 新增工程详情页，只展示功能模块列表与创建模块入口
- [x] 新增独立创建模块页
- [x] 新增功能模块主页，同页展示“已采纳结果全文 + 历史生成任务列表”
- [x] 新增单次任务详情页，用于完整审阅、冲突追溯查看与采纳动作
- [x] 恢复旧的 `/requirement-generation` 与 `/detail-design-generation` 页面为真实生成工具页
- [x] 将旧生成页绑定到 `projectId + moduleId` 上下文
- [x] 让旧生成页生成完成后回跳到功能模块主页，并高亮新任务
- [x] 补充 breadcrumb 层级导航
- [x] 创建开发分支 `codex-layered-navigation-workflow`
- [x] 创建提交 `8bfcd66 Implement layered project module task navigation`
- [ ] 在真实浏览器里手动走通“工程 -> 模块 -> 生成页 -> 模块页 -> 任务详情页”全链路
- [ ] 评估是否需要把任务详情页里的审阅交互与旧页面视觉进一步对齐
- **状态：** in_progress

### 阶段 7：后续扩展
- [ ] 为功能模块主页补更多摘要信息，如资产统计、最近一次生成时间、按文档类型分组
- [ ] 视需要补充任务详情页的输入资产明细与差异对比
- [ ] 打通模块主页与 Skill Refinement 的更清晰信息联动，但保持入口独立
- [ ] 继续修复核心文档的编码一致性问题
- [ ] 整理新导航结构下的最终使用说明与交付说明
- **状态：** pending

## 关键问题
1. 如何在不破坏旧生成页内容区的前提下，把系统改造成明确的层级导航？
2. 如何让功能模块页承担“主浏览页”职责，而旧生成页只承担“生成工具页”职责？
3. 如何保证模块级任务、已采纳结果、旧生成页和任务详情页共享同一套数据源？
4. 如何把已经迁移的演示工程继续复用于新导航结构，而不是重复造一套数据？
5. 如何在换环境继续开发时，最快恢复“当前分支、当前提交、当前阶段、未完成事项”这些关键上下文？

## 已做决策
| 决策 | 理由 |
|------|------|
| 首页只保留工程列表，不再堆叠模块、任务、资产和结果 | 用户明确不接受“主界面一股脑堆内容”的信息架构 |
| 创建工程与创建模块都改为独立页面 | 降低层级混乱感，符合“先选工程，再进工程，再建模块”的心智模型 |
| 功能模块主页作为主浏览页 | 模块页最适合集中看已采纳结果与历史任务 |
| 已采纳结果在模块主页默认直接展示全文 | 用户希望在模块层级直观看到已采纳需求，不只是摘要 |
| 单次任务新增独立详情页 | 避免模块主页继续膨胀，同时给冲突/追溯/采纳动作留足空间 |
| 旧生成页只做生成，不再承担主审阅入口 | 用户明确要求“生成页就只是做生成页” |
| 旧生成页恢复为真实页面，而不是跳首页 | 用户要保留旧页面的原始结果展示能力 |
| 旧生成页按 `projectId + moduleId` 上下文过滤 | 从模块页进入后，必须只操作当前模块的数据 |
| 生成完成后默认返回模块主页 | 生成后的主观察面应该是模块主页的历史任务列表 |
| Skill Refinement 继续保持独立顶层入口 | 用户当前没有要求把它并入工程/模块层级 |
| 本轮代码整理在 `codex-layered-navigation-workflow` 分支提交 | 便于换环境继续开发与回看本轮页面架构变更 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `git switch -c codex/...` 失败，提示无法创建 `refs/heads/codex/...` | 1 | 改用不带斜杠的分支名 `codex-layered-navigation-workflow` |
| sandbox 下创建新 git branch 失败，提示 `.git/refs/heads/*.lock` 无权限 | 1 | 提权执行 `git switch -c` 创建并切换分支 |
| 旧生成页被改成首页跳转页后，不再满足用户对旧审阅信息完整性的要求 | 1 | 回退思路，恢复旧生成页为真实工具页，并新增独立任务详情页承担审阅 |
| 新生成工具页缺少 breadcrumb 和多选资产样式 | 1 | 在 `public/app.css` 做最小量增补，不改旧内容区主体结构 |

## 备注
- 当前最新开发分支：`codex-layered-navigation-workflow`
- 当前最新提交：`8bfcd66 Implement layered project module task navigation`
- 当前仍保留迁移演示工程，可用于换环境后的页面联调与人工走查
- 自动化测试当前通过：`npm test` -> `All 10 tests passed.`
- 下一次接手开发前，优先读取 `task_plan.md`、`findings.md`、`progress.md`，再看 `git status` 与当前分支/最近提交
