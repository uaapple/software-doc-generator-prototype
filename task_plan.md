# 任务计划：软件需求生成平台产品化完善

## 目标
在保留当前需求生成主链路可用的前提下，持续完善内部工作台的可用性、品牌一致性与 LLM 接入能力，使平台既能稳定生成结构化需求草案，也能支持多模型切换、驳回反馈沉淀与后续服务商扩展。

## 当前阶段
阶段 4

## 各阶段

### 阶段 1：需求与发现
- [x] 理解用户对内部工具工作台的定位
- [x] 明确首页需要保留品牌识别，但避免宣传页风格
- [x] 明确 LLM 接入需要支持可切换、可新增、可扩展
- [x] 将关键发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：页面结构与品牌收敛
- [x] 将首页从展示型 hero 调整为内部工作台布局
- [x] 接入 ITK logo，并按品牌蓝灰色系统一视觉
- [x] 修复中文乱码与基础信息层级
- [x] 调整上传控件风格，统一按钮与表单视觉语言
- [x] 修复卡片宽度不一致问题，统一为两列等宽栅格
- [x] 将 Skill Refinement 工作台重做为与首页一致的内部系统风格
- **状态：** complete

### 阶段 3：LLM 配置与后端接入
- [x] 新增 LLM 配置持久化存储
- [x] 增加模型配置查询、新增、默认切换接口
- [x] 首批支持 OpenAI 与豆包两类 provider
- [x] 将生成链路改为按所选模型配置调用
- [x] 记录最近一次生成所使用的模型信息
- [x] 保留并自动补回默认豆包 seed 配置
- **状态：** complete

### 阶段 4：联调、回归与反馈闭环设计
- [x] 补充模型配置相关自动化测试
- [x] 验证测试入口可正常通过
- [x] 修复“服务商下拉为空”的前端联调问题
- [x] 记录当前项目状态与进度
- [x] 核对前后端实现、测试入口与计划文件是否一致
- [ ] 在运行中的本地服务里人工确认新增模型 UI 表现与交互闭环
- [ ] 基于真实页面再做一轮细节收口
- [ ] 修复项目核心文档在当前环境中的乱码/编码一致性问题
- [ ] 设计“驳回原因 + 驳回记录池 + 回投大模型修正 skill”闭环能力
- [ ] 明确驳回记录的分组机制、回投触发方式和物料结构
- **状态：** in_progress

### 阶段 5：后续扩展
- [ ] 预置更多 provider 入口，如 GLM / Minimax / Kimi
- [ ] 为不同 provider 补充更细的接入参数规则
- [ ] 考虑补充模型配置编辑、删除与连接测试能力
- [ ] 实现驳回池浏览、分组筛选和再次投喂大模型的界面与接口
- [ ] 整理最终使用说明与交付说明
- **状态：** pending

## 关键问题
1. 如何在不破坏当前主链路的前提下，把首页收敛成真正适合内部使用的工作台？
2. 如何把 LLM 配置做成可扩展 provider 注册表，而不是只为 OpenAI / 豆包写死一次？
3. 如何确保仓库中已有的默认豆包配置不会因为新存储逻辑被覆盖或丢失？
4. 如何让前端模型配置区在首次加载、历史空配置文件存在时仍能自愈显示？
5. 如何把“人工驳回原因”转化为可持续积累、可再次投喂大模型的 skill 修正物料？

## 已做决策
| 决策 | 理由 |
|------|------|
| 首页改为内部工作台样式而不是宣传页 | 贴合真实使用场景，减少冗余文案与展示感 |
| 统一四个主功能卡片为两列等宽栅格 | 保持信息块整齐，降低界面“没对齐”的违和感 |
| 新增 `llm-profile-service` 管理 provider 与 profile | 让模型配置具备持久化与后续扩展能力 |
| 生成时按 `llmProfileId` 解析 client，而不是只读环境变量 | 支持不同模型配置切换与多 provider 并存 |
| 保留 `.env.defaults` 中的豆包配置作为 seed profile | 避免原有可用配置被新功能破坏 |
| 前端单独请求 `/api/llm-profiles` | 降低对 `/api/meta` 聚合结构的耦合，修复服务商下拉空白问题 |
| 新增功能需求优先按“反馈闭环”思路设计，而不是只增加一个备注字段 | 后续目标是把驳回信息变成可回投的大模型修正物料 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Windows 下 `apply_patch` 在当前工作区异常，无法直接打补丁 | 1 | 改用 PowerShell 直接写入文件继续推进 |
| `planning-with-files` 的 `session-catchup.py` 调用失败，系统提示 `python` 不在 PATH 中 | 1 | 记录问题，改为直接读取计划文件与 `git diff --stat` 对齐当前状态 |
| 前端“服务商”下拉为空 | 1 | 前端改为单独请求 `/api/llm-profiles`，后端初始化逻辑增加 seed profile 自愈 |
| `npm test` 在当前 PowerShell 环境触发 `npm.ps1` 执行策略错误 | 1 | 改为直接运行 `node tests/run-tests.js` 确认测试通过 |

## 备注
- 当前主线任务已从“需求生成规则调优”扩展到“内部工作台可用性 + 多模型接入能力 + 人工反馈闭环设计”。
- 自动化测试已通过，但仍缺少对运行中页面的人工交互确认。
- README / STATUS 文档在当前环境读取乱码，说明除代码外还存在一项交付层面的可维护性问题。
- 后续如果继续扩 provider，优先沿用现有 provider 注册表与 profile 存储结构。
