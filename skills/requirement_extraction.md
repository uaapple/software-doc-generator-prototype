# 事实抽取 Skill

## 任务目标
- 从系统需求 PDF、模型 PDF、生成 C 文件中抽取可形成软件需求的事实。
- 优先识别带有明确行为、条件、接口、状态、阈值、周期、故障处理的信息。
- 不直接编造需求，不把没有证据的推断写成事实。

## 抽取规则
- 若文本本身带有“应”“必须”“shall”“must”等措辞，优先视为高价值候选事实。
- 从模型侧输入中提取实现细节，但不能覆盖系统需求。
- 对同一事实的多来源证据应保留来源位置，用于后续追溯。
- 对存在冲突的事实保留差异，不在本阶段裁决。
