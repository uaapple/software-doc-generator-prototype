interface:
  display_name: "Simulink UT TCSD Generator"
  short_description: "Generate and repair coverage-closed unit-test TCSD Excel cases from Simulink SLX/MAT models."
  default_prompt: "Use this skill to generate coverage-first Simulink unit-test TCSD cases from a .slx model and matching .mat file; assume the bundled support-package copy, SATK/MCP/MATLAB model reading as the authority, repair SATK tools path after restoredefaultpath before declaring model_read/model_overview unavailable, static SLX XML only as a post-SATK supplement via scripts/inspect_slx_xml.py, decision-coverage focus, Logical Operator AND/OR MC/DC truth-vector coverage, Abs pre-source sign coverage, Switch/Relational trigger true-false coverage, Saturate pre-limit input proof or unreachable justification, Stateflow/state/mode transition path tracing before drafting, conservative transition hold timing instead of assuming one request signal is enough, hard semantic consistency gate between state-transition claims and simulation-backed expValue outputs, default all-scalar-root-output simulation backfill with stateful exclusions, minimum functional-domain coverage density, vector root-input element assignments, final action delay markers, coverage-feedback closure with probes/reruns, canonical Excel template assets/templates/tcsd_template.xlsx, top-level-Outport-only expectations, simulation backfill, outputs/<model>_Test0001_tcsd.xlsx workbook, JSON spec, simulation JSON, and validation report are required by default."
