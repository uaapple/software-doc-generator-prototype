interface:
  display_name: "Simulink 模块功能描述 · 阶段 1 初始化"
  short_description: "校验任务输入并初始化任务独占 MATLAB 会话与模型"
  default_prompt: "Use $software-detail-stage-01-initialize to initialize the pipeline job from its supplied model, workspace, and addon inputs."

policy:
  allow_implicit_invocation: false
